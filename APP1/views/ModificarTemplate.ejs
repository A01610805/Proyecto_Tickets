<%- include('includes/head.ejs') %>

    <div class="container">

        <h3>Escoger Categoría a Modificar</h3>
        <ul class="collection">
            <% for (let categoria of categorias) { %>
                <% if ( categoria.disponible == 1) { %>
                    <li class="">
                        <a href="/modificar_template/get_preg/<%= categoria.ID_categoria %>/<%= categoria.nombre_categoria %>/<%= categoria.tiempo_estimado %>" class="collection-item">
                            <%= categoria.nombre_categoria %>
                        </a>
                    </li>
                <% } %>
            <% } %>
        </ul>

    </div>

    <%- include('includes/footer.ejs') %>